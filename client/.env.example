# Base URL of the backend API. Vite only exposes env vars prefixed with VITE_.
VITE_API_URL=http://localhost:5000/api

# Base URL for the Socket.IO connection - no "/api" suffix, since this isn't
# a REST endpoint.
VITE_SOCKET_URL=http://localhost:5000
