# Port the Express server listens on
PORT=5000

# MongoDB connection string (get one free from MongoDB Atlas)
MONGO_URI=mongodb+srv://ahteshan_04:ahteshan0904@cluster0.poux9.mongodb.net/collabboard?retryWrites=true&w=majority&appName=Cluster0

# Secret used to sign JWT tokens. Use a long random string.
JWT_SECRET=replace_this_with_a_long_random_string

# URL of the React frontend, used for CORS + cookie settings
CLIENT_URL=http://localhost:5173
